# 🔍 БЫЛ ЛИ ОРКЕСТРАТОР В ТЗ?

## 📋 ЧТО Я НАШЁЛ

### В коде есть:

1. **EDEM ORCHESTRA** (`src/lib/edem-orchestra.ts`)
   - Автоматический выбор голоса на основе анализа сообщения
   - Реализован и подключён в API

2. **UI для ручного выбора** (`src/components/ChatPanel.tsx`)
   - Пользователь может выбрать голос вручную
   - Показываются 3 голоса (live, shadow, mirror)

3. **Конфликт:**
   - UI позволяет выбрать голос вручную
   - Но в `page.tsx` всегда отправляется `autoSelectVoice: true`
   - **Результат:** ручной выбор игнорируется

---

## ❓ ВОПРОС: БЫЛ ЛИ ОРКЕСТРАТОР В ИСХОДНОМ ТЗ?

### Варианты:

**Вариант А: Оркестратор БЫЛ в ТЗ**
- Тогда проблема: ручной выбор конфликтует с автоматическим
- Нужно: убрать UI для ручного выбора ИЛИ сделать его опциональным

**Вариант Б: Оркестратор НЕ БЫЛ в ТЗ**
- Тогда проблема: он был добавлен позже и конфликтует с ручным выбором
- Нужно: либо убрать оркестратор, либо сделать его опциональным

**Вариант В: Оркестратор БЫЛ в ТЗ, но ручной выбор ТОЖЕ БЫЛ**
- Тогда проблема: они конфликтуют в реализации
- Нужно: исправить логику, чтобы ручной выбор имел приоритет

---

## 🔍 ЧТО ГОВОРИТ КОД

### В `EDEM_ORCHESTRA_COMPLETE.md`:
- "EDEM ORCHESTRA — Реализация завершена"
- "Включён по умолчанию (`autoSelectVoice: true`)"
- "Работает автоматически при каждом сообщении"

### В `ChatPanel.tsx`:
- Есть UI для выбора голоса
- Пользователь может выбрать голос
- Но выбор не используется из-за `autoSelectVoice: true`

### В `page.tsx`:
```typescript
autoSelectVoice: true, // ВСЕГДА true!
```
- Всегда отправляется `autoSelectVoice: true`
- Выбранный пользователем голос игнорируется

---

## 💡 МОЯ ГИПОТЕЗА

**Судя по коду, оркестратор был добавлен позже:**
1. Сначала был реализован ручной выбор (UI в `ChatPanel.tsx`)
2. Потом был добавлен оркестратор (`edem-orchestra.ts`)
3. Оркестратор был включён по умолчанию (`autoSelectVoice: true`)
4. Но логика не была обновлена, чтобы учитывать ручной выбор
5. **Результат:** конфликт между ручным и автоматическим выбором

---

## ✅ ЧТО НУЖНО СДЕЛАТЬ

### Если оркестратор БЫЛ в ТЗ:
- Убрать UI для ручного выбора (или сделать его опциональным)
- Оставить только автоматический выбор

### Если оркестратор НЕ БЫЛ в ТЗ:
- Убрать оркестратор
- Оставить только ручной выбор

### Если оба БЫЛИ в ТЗ:
- Исправить логику: ручной выбор имеет приоритет
- Автоматический выбор только если голос не выбран

---

## 🎯 РЕКОМЕНДАЦИЯ

**Спросить у пользователя:**
1. Был ли оркестратор в исходном ТЗ?
2. Должен ли быть ручной выбор голоса?
3. Какой приоритет: ручной или автоматический?

**После ответа — исправить логику соответственно.**

