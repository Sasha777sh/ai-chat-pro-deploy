# 📋 Инструкция по миграции данных из старого проекта

## ✅ Что уже сделано

1. ✅ Миграция обновлена для совместимости со старым проектом
2. ✅ Миграция сохраняет существующие данные
3. ✅ Добавляет новые поля и таблицы без удаления старых

## 🚀 Как запустить миграцию

### Шаг 1: Открой Supabase Dashboard

1. Иди на https://supabase.com/dashboard
2. Выбери проект: `jgnnrdrqzcwnhuuvhlfo`

### Шаг 2: Запусти миграцию

1. Открой **SQL Editor** (иконка `</>` в боковом меню)
2. Нажми **"New query"**
3. Скопируй содержимое файла `supabase/migrations/001_init.sql`
4. Вставь в SQL Editor
5. Нажми **"Run"** (или Ctrl+Enter)

### Шаг 3: Проверь результат

1. Открой **Table Editor** в Supabase
2. Проверь что таблицы созданы:
   - ✅ `profiles`
   - ✅ `chat_sessions`
   - ✅ `chat_messages`

## 🔄 Что делает миграция

- ✅ Сохраняет существующие данные из `profiles`
- ✅ Добавляет поле `subscription_tier` (если нет)
- ✅ Создаёт новые таблицы `chat_sessions` и `chat_messages`
- ✅ Настраивает RLS политики для безопасности
- ✅ Создаёт триггер для автоматического создания профилей

## 📝 Примечание

Если в старом проекте были данные в таблице `sessions`, они сохранятся, но новая структура использует `chat_sessions`. Старые данные можно будет перенести позже если нужно.

