# 📝 Промпты ИИ в проекте

## 🔍 Где находятся промпты

### Основной промпт чата

**Файл:** `src/app/api/chat/route.ts`  
**Строки:** 73-79

```typescript
const systemPrompt = `Ты — ИИ-зеркало для самопознания. Твоя задача:
- Видеть и отражать истину без оценок
- Помогать человеку понять себя через вопросы и наблюдения
- Быть мягким, но честным
- Помнить контекст разговора

Отвечай на русском языке, коротко и по существу.`;
```

---

## 🎯 Параметры OpenAI

**Файл:** `src/app/api/chat/route.ts`  
**Строки:** 91-96

```typescript
const completion = await openai.chat.completions.create({
  model: 'gpt-4o-mini',
  messages: messages as any,
  temperature: 0.7,
  max_tokens: 500,
});
```

**Параметры:**
- **model:** `gpt-4o-mini` (быстрая и дешёвая модель)
- **temperature:** `0.7` (баланс между креативностью и точностью)
- **max_tokens:** `500` (максимум символов в ответе)

---

## ✏️ Как изменить промпт

1. Открой `src/app/api/chat/route.ts`
2. Найди строку 73: `const systemPrompt = ...`
3. Измени текст промпта
4. Сохрани и задеплой:
   ```bash
   git add .
   git commit -m "Update AI prompt"
   git push
   ```

---

## 🔄 История разговора

ИИ получает последние 50 сообщений из истории (строки 65-70):

```typescript
const { data: history } = await supabase
  .from('chat_messages')
  .select('role, content')
  .eq('session_id', sessionId)
  .order('created_at', { ascending: true })
  .limit(50);
```

---

## 💡 Примеры промптов

### Более дружелюбный:
```
Ты — добрый друг и помощник. Отвечай дружелюбно, поддерживай человека, давай полезные советы.
```

### Более строгий:
```
Ты — честный наставник. Говори правду напрямую, не смягчай. Помогай расти через критику.
```

### Для бизнеса:
```
Ты — бизнес-консультант. Помогай принимать решения, анализировать риски, планировать стратегию.
```

---

## 📊 Мониторинг промптов

Админ-панель: https://chatedem.com/admin

Там можно:
- Видеть текущий промпт
- Смотреть статистику использования
- Управлять пользователями

