# 🔧 Исправление проблемы с редиректом после регистрации

## 🐛 Проблема

После регистрации пользователь не попадает в чат, система возвращает на страницу регистрации или ввода пароля.

## 🔍 Причина

1. **Сессия не успевает установиться** — после `signUp()` cookies могут устанавливаться с задержкой
2. **Chat page проверяет auth слишком рано** — до того как cookies установлены
3. **Нет проверки сессии** — только проверка user, но не session

## ✅ Что исправлено

### 1. Ожидание установки сессии после регистрации
**Файл:** `src/app/signup/page.tsx`

- Добавлено ожидание 500ms после регистрации
- Проверка сессии через `getSession()` перед редиректом
- Логирование для отладки

### 2. Улучшена проверка auth в chat page
**Файл:** `src/app/chat/page.tsx`

- Сначала проверяется `getSession()`
- Если нет session, но есть user — обновляем сессию
- Добавлено логирование всех проверок
- Улучшена обработка ошибок при создании сессии

### 3. Улучшена функция createOrGetSession
- Используется `maybeSingle()` вместо `single()` (не падает если нет данных)
- Добавлен try-catch для обработки ошибок
- Логирование для отладки

## 🧪 Как проверить

1. Зарегистрируй нового пользователя
2. Проверь консоль браузера — должны быть логи:
   - `Signup data: { user: '...', session: true/false }`
   - `Auto-login successful, waiting for session to be set...`
   - `Session confirmed, redirecting to chat`
   - `Auth check passed, user: ...`

3. Если всё работает:
   - Должен быть автоматический переход в `/chat`
   - Не должно быть редиректа обратно на `/login` или `/signup`

## ⚠️ Если проблема остаётся

### Проверь в Supabase Dashboard:
1. **Authentication → Settings:**
   - Проверь, включено ли "Enable email confirmations"
   - Если включено — отключи для тестирования или подтверди email

2. **Authentication → Users:**
   - Проверь, создался ли пользователь
   - Проверь статус (confirmed/unconfirmed)

3. **Database → Tables → profiles:**
   - Проверь, создался ли профиль автоматически

### Проверь в консоли браузера:
- Открой DevTools → Console
- Выполни регистрацию
- Посмотри все логи
- Если есть ошибки — зафиксируй их

### Проверь Network tab:
- Открой DevTools → Network
- Выполни регистрацию
- Проверь запросы к Supabase
- Проверь статусы ответов

## 📝 Дополнительные улучшения

Если проблема остаётся, можно добавить:
1. Retry механизм для проверки сессии
2. Показ loading состояния во время ожидания
3. Fallback на ручной вход если автоматический не сработал

