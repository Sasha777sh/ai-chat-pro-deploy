# 📋 Инструкция для ассистента: Завершение AI Chat Pro

## 🎯 КОНТЕКСТ ПРОЕКТА

**Что это:**
AI Chat Pro — полноценный SaaS-сервис для общения с ИИ-зеркалом. Пользователи регистрируются, общаются с ИИ (с памятью), могут купить подписку Pro.

**Технологии:**

- Next.js 15 + TypeScript + Tailwind CSS
- Supabase (PostgreSQL + Auth)
- OpenAI API (GPT-4o-mini)
- Stripe (платежи)

**Где находится проект:**

```
/Users/sanecek/tema/ai-chat-pro/
├── src/app/              ← Страницы и API
├── supabase/migrations/  ← SQL миграции БД
└── .env.local           ← Ключи (уже настроены)
```

---

## ✅ ЧТО УЖЕ СДЕЛАНО

1. ✅ **Структура проекта** — все файлы созданы
2. ✅ **Лендинг** — `/` (главная страница)
3. ✅ **Регистрация/Вход** — `/signup`, `/login`
4. ✅ **Чат с ИИ** — `/chat` (с сохранением истории)
5. ✅ **Личный кабинет** — `/account` (с тарифами)
6. ✅ **API роуты** — `/api/chat`, `/api/stripe/*`
7. ✅ **Миграции БД** — `supabase/migrations/001_init.sql` (готовы)
8. ✅ **Ключи настроены** — `.env.local` с Supabase и OpenAI
9. ✅ **Сервер запущен** — работает на <http://localhost:3000>

---

## 🔧 ЧТО НУЖНО ДОДЕЛАТЬ

### ЗАДАЧА 1: Запустить миграции БД в Supabase

**Файл:** `supabase/migrations/001_init.sql`

**Что делать:**

1. Открой <https://supabase.com/dashboard>
2. Войди в проект: `jgnnrdrqzcwnhuuvhlfo`
3. Открой **SQL Editor** (иконка `</>` в боковом меню)
4. Нажми **"New query"**
5. Открой файл `ai-chat-pro/supabase/migrations/001_init.sql`
6. Скопируй ВСЁ содержимое файла
7. Вставь в SQL Editor
8. Нажми **"Run"** (или Ctrl+Enter)
9. Дождись выполнения (должно быть "Success")

**Проверка:**

- Открой **Table Editor** в Supabase
- Должны быть таблицы: `profiles`, `chat_sessions`, `chat_messages`
- Если есть ошибки — исправь их и запусти снова

**Результат:** База данных готова, таблицы созданы, RLS политики настроены.

---

### ЗАДАЧА 2: Проверить что всё работает локально

**Что делать:**

1. Убедись что сервер запущен:

   ```bash
   cd /Users/sanecek/tema/ai-chat-pro
   npm run dev
   ```

2. Открой <http://localhost:3000>
3. Проверь страницы:
   - ✅ Лендинг открывается
   - ✅ `/signup` — форма регистрации
   - ✅ `/login` — форма входа
4. Зарегистрируй тестового пользователя:
   - Email: `test@example.com`
   - Пароль: `test123456`
5. После регистрации должен перенаправить в `/chat`
6. Проверь чат:
   - Напиши сообщение
   - ИИ должен ответить
   - Сообщения должны сохраняться

**Если ошибки:**

- Проверь `.env.local` — все ключи должны быть
- Проверь консоль браузера (F12) — нет ли ошибок
- Проверь терминал где запущен `npm run dev` — нет ли ошибок
- Проверь Supabase Dashboard → Logs — нет ли ошибок БД

**Результат:** Всё работает локально, можно регистрироваться и общаться с ИИ.

---

### ЗАДАЧА 3: Деплой на Vercel

**Файл:** `DEPLOY.md` (подробная инструкция)

**Что делать:**

1. Создай GitHub репозиторий и закоммить проект
2. Иди на <https://vercel.com> и импортируй проект
3. Добавь все переменные окружения (см. `DEPLOY.md`)
4. Настрой webhook'и для платежей (Stripe, ЮKassa, Crypto)
5. Задеплой проект

**Результат:** Сайт работает в продакшене на Vercel.

---

### ЗАДАЧА 4: Настроить платёжки (опционально, но рекомендуется)

**Доступные платёжки:**

- ✅ **Stripe** — карты (международные)
- ✅ **ЮKassa** — карты (Россия)
- ✅ **Криптоплатежи** — Bitcoin, USDT и т.д.

**Что делать:**

1. **Stripe:**
   - Создай аккаунт на <https://stripe.com>
   - Получи ключи (test для разработки, live для продакшена)
   - Настрой webhook в Stripe Dashboard
   - Добавь ключи в Vercel Environment Variables

2. **ЮKassa:**
   - Создай аккаунт на <https://yookassa.ru>
   - Получи Shop ID и Secret Key
   - Настрой webhook в ЮKassa Dashboard
   - Добавь ключи в Vercel Environment Variables

3. **Криптоплатежи:**
   - Выбери провайдера (NOWPayments, CoinGate и т.д.)
   - Настрой IPN webhook
   - Добавь IPN secret в Vercel Environment Variables

**Результат:** Все способы оплаты работают, пользователи могут покупать Pro подписку.

**Что делать:**

1. Создай GitHub репозиторий (если нет):

   ```bash
   cd /Users/sanecek/tema/ai-chat-pro
   git init
   git add .
   git commit -m "Initial commit"
   # Создай репозиторий на GitHub и подключи
   ```

2. Иди на <https://vercel.com>
3. Импортируй проект из GitHub
4. Укажи корневую папку: `ai-chat-pro` (или оставь пустым если репозиторий только для этого проекта)
5. Добавь переменные окружения в Vercel:
   - `NEXT_PUBLIC_SUPABASE_URL` = `https://jgnnrdrqzcwnhuuvhlfo.supabase.co`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = (из `.env.local`)
   - `SUPABASE_SERVICE_ROLE_KEY` = (из `.env.local`)
   - `OPENAI_API_KEY` = (из `.env.local`)
   - `NEXT_PUBLIC_APP_URL` = `https://your-project.vercel.app`
   - `STRIPE_SECRET_KEY` = (опционально, если настроен Stripe)
   - `STRIPE_WEBHOOK_SECRET` = (опционально)

6. Нажми **Deploy**
7. Дождись завершения деплоя

**Проверка:**

- Открой задеплоенный сайт
- Проверь что всё работает как локально

**Результат:** Сайт задеплоен и работает в продакшене.

---

## 📁 СТРУКТУРА ПРОЕКТА (для справки)

```
ai-chat-pro/
├── src/
│   ├── app/
│   │   ├── page.tsx              # Лендинг
│   │   ├── login/page.tsx        # Вход
│   │   ├── signup/page.tsx       # Регистрация
│   │   ├── chat/page.tsx         # Чат с ИИ
│   │   ├── account/page.tsx      # Личный кабинет
│   │   └── api/
│   │       ├── chat/route.ts     # API чата (с памятью)
│   │       └── stripe/           # Платежи (checkout, webhook)
│   └── lib/
│       └── supabase.ts           # Supabase клиент
├── supabase/
│   └── migrations/
│       └── 001_init.sql          # Миграция БД (ЗАПУСТИТЬ!)
├── .env.local                    # Ключи (уже настроены)
└── package.json
```

---

## 🔑 КЛЮЧИ (уже настроены в .env.local)

- `NEXT_PUBLIC_SUPABASE_URL` = `https://jgnnrdrqzcwnhuuvhlfo.supabase.co`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` = (есть в .env.local)
- `SUPABASE_SERVICE_ROLE_KEY` = (есть в .env.local)
- `OPENAI_API_KEY` = (есть в .env.local)

**Не коммить `.env.local` в Git!**

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Миграции БД** — ОБЯЗАТЕЛЬНО запустить в Supabase SQL Editor перед использованием
2. **RLS политики** — уже настроены в миграции, пользователи видят только свои данные
3. **Лимиты** — Free: 10 сообщений/день, Pro: безлимит (проверяется в `/api/chat`)
4. **Память ИИ** — загружается последние 50 сообщений из БД для контекста
5. **Stripe** — опционально, можно настроить позже для реальных платежей

---

## ✅ ЧЕКЛИСТ ЗАВЕРШЕНИЯ

- [ ] Миграции БД запущены в Supabase
- [ ] Таблицы созданы (`profiles`, `chat_sessions`, `chat_messages`)
- [ ] Локально всё работает (регистрация, вход, чат)
- [ ] ИИ отвечает в чате
- [ ] Сообщения сохраняются в БД
- [ ] Личный кабинет показывает тариф
- [ ] (Опционально) Задеплоено на Vercel

---

## 🚨 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

1. **Ошибка авторизации:**
   - Проверь `.env.local` — ключи Supabase правильные?
   - Проверь Supabase Dashboard → Settings → API

2. **Ошибка чата:**
   - Проверь `.env.local` — OpenAI ключ правильный?
   - Проверь консоль браузера (F12) — какая ошибка?
   - Проверь терминал сервера — есть ли ошибки?

3. **Ошибка БД:**
   - Проверь что миграции запущены
   - Проверь Supabase Dashboard → Table Editor — таблицы есть?
   - Проверь Supabase Dashboard → Logs — есть ли ошибки?

4. **Страница не открывается:**
   - Проверь что `npm run dev` запущен
   - Проверь что порт 3000 свободен
   - Проверь консоль терминала — есть ли ошибки компиляции?

---

## 📝 ФИНАЛЬНЫЙ ШАГ

После того как всё работает:

1. Сообщи пользователю что готово
2. Дай ссылку на сайт (локальную или Vercel)
3. Объясни что можно делать:
   - Регистрироваться
   - Общаться с ИИ
   - Покупать подписку Pro (если Stripe настроен)

---

**Удачи! Проект почти готов, осталось только запустить миграции БД и проверить работу.**
