# 🏗️ Архитектура AI Chat Pro

## 🎯 Что мы строим

**Полноценный SaaS-сервис** для общения с ИИ-зеркалом:
- Лендинг с тарифами
- Регистрация/авторизация
- Чат с ИИ (с памятью)
- Личный кабинет
- Оплата подписки
- История всех разговоров

---

## 🛠️ Технологический стек

### Frontend
- **Next.js 15** — React фреймворк (SSR, API routes)
- **TypeScript** — типобезопасность
- **Tailwind CSS** — современный дизайн
- **Supabase JS** — клиент для БД и Auth

### Backend
- **Next.js API Routes** — серверные эндпоинты
- **Supabase** — PostgreSQL + Auth + RLS
- **OpenAI API** — GPT-4 для чата
- **Stripe** — платежи (подписки)

### База данных (Supabase)
```sql
profiles
  - id (uuid, primary)
  - email
  - subscription_tier (free/pro)
  - subscription_expires_at
  - created_at

chat_sessions
  - id (uuid, primary)
  - user_id (uuid, foreign)
  - title
  - created_at
  - updated_at

chat_messages
  - id (uuid, primary)
  - session_id (uuid, foreign)
  - role (user/assistant)
  - content (text)
  - created_at
```

---

## 🔄 Поток работы

### 1. Регистрация
```
Пользователь → /signup → Supabase Auth → 
Создаётся профиль (tier: free) → 
Перенаправление в /chat
```

### 2. Чат с ИИ
```
Пользователь пишет → /api/chat →
Проверка лимита (free: 10/день) →
Загрузка истории сессии из БД →
Отправка в OpenAI (контекст + история) →
Сохранение ответа в БД →
Возврат ответа клиенту
```

### 3. Оплата
```
Пользователь → /account → "Upgrade to Pro" →
Stripe Checkout → Webhook →
Обновление subscription_tier в БД →
Доступ к безлимиту
```

---

## 📦 Структура проекта

```
ai-chat-pro/
├── src/
│   ├── app/
│   │   ├── page.tsx              # Лендинг
│   │   ├── login/page.tsx        # Вход
│   │   ├── signup/page.tsx       # Регистрация
│   │   ├── chat/page.tsx         # Чат с ИИ
│   │   ├── account/page.tsx      # Личный кабинет
│   │   └── api/
│   │       ├── chat/route.ts     # API чата
│   │       └── stripe/...        # Платежи
│   ├── components/
│   │   ├── ChatMessage.tsx
│   │   └── SubscriptionButton.tsx
│   └── lib/
│       ├── supabase.ts           # Supabase клиент
│       ├── openai.ts             # OpenAI клиент
│       └── stripe.ts             # Stripe клиент
├── supabase/
│   └── migrations/               # SQL миграции
└── package.json
```

---

## 💰 Монетизация

| Тариф | Лимит | Цена |
|-------|-------|------|
| **Free** | 10 сообщений/день | 0₽ |
| **Pro** | Безлимит | 990₽/мес |

---

## 🔐 Безопасность

- **RLS (Row Level Security)** в Supabase — пользователь видит только свои данные
- **API ключи** в `.env.local` (не коммитятся)
- **JWT токены** Supabase для авторизации
- **Rate limiting** на API (по тарифу)

---

## 🚀 Деплой

1. **Vercel** — фронтенд + API routes
2. **Supabase Cloud** — БД + Auth
3. **Stripe** — платежи (встроенный)

---

## 📝 Что дальше

1. ✅ Создать структуру проекта
2. ✅ Настроить Supabase (БД + миграции)
3. ✅ Реализовать чат с памятью
4. ✅ Интегрировать Stripe
5. ✅ Деплой на Vercel

