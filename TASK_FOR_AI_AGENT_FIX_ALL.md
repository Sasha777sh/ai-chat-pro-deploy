# 🔧 ЗАДАЧА ДЛЯ ИИ-АГЕНТА: Исправить все ошибки и запустить систему

## 🎯 ЦЕЛЬ
Исправить все критические ошибки в системе EDEM Intelligence и обеспечить работоспособность на продакшене (chatedem.com).

---

## ⚡ БЫСТРЫЙ СТАРТ (выполни по порядку)

### ШАГ 1: Проверь текущее состояние проекта

```bash
cd /Users/sanecek/tema/ai-chat-pro
git status
git log --oneline -5
```

**Что проверить:**
- Есть ли незакоммиченные изменения?
- Какой последний коммит?

---

### ШАГ 2: Проверь код на ошибки компиляции

```bash
cd /Users/sanecek/tema/ai-chat-pro
npm run build
```

**Если есть ошибки:**
- Исправь их
- Запусти `npm run build` снова
- Повторяй до успешной сборки

---

### ШАГ 3: Проверь критический файл аутентификации

**Файл:** `src/app/api/chat/stream/route.ts`

**Проверь строки 68-72:**

```typescript
// ✅ ДОЛЖНО БЫТЬ ТАК:
const supabaseAuth = createClient(supabaseUrl, supabaseAnonKey);

// ❌ НЕ ДОЛЖНО БЫТЬ ТАК:
// const supabaseAuth = createClient(supabaseUrl, supabaseServiceKey);
```

**Если неправильно:**
- Исправь на `supabaseAnonKey`
- Сохрани файл

---

### ШАГ 4: Проверь все API роуты на правильную аутентификацию

**Проверь эти файлы:**

1. `src/app/api/yookassa/checkout/route.ts`
2. `src/app/api/crypto/checkout/route.ts`
3. `src/app/api/payment/manual/route.ts`

**В каждом файле должно быть:**

```typescript
// Для проверки пользовательского токена:
const supabaseAuth = createClient(supabaseUrl, supabaseAnonKey);
const { data: { user }, error: authError } = await supabaseAuth.auth.getUser(token);

// Для работы с данными (после проверки):
const supabase = createClient(supabaseUrl, supabaseServiceKey);
```

**Если неправильно:**
- Исправь все файлы
- Сохрани изменения

---

### ШАГ 5: Закоммить все исправления

```bash
cd /Users/sanecek/tema/ai-chat-pro
git add .
git commit -m "Fix: исправлена аутентификация во всех API роутах"
git push origin main
```

**Проверь:**
- Все изменения закоммичены?
- Код запушен в репозиторий?

---

### ШАГ 6: Проверь подключение Vercel к Git

1. Открой: https://vercel.com/dashboard
2. Найди проект с доменом `chatedem.com`
3. Проверь: Settings → Git → Connected Git Repository
4. Если репозиторий не подключен:
   - Нажми "Connect Git Repository"
   - Выбери репозиторий
   - Подключи

**Проверь:**
- Репозиторий подключен?
- Какая ветка используется для деплоя? (должна быть `main`)

---

### ШАГ 7: Проверь переменные окружения на Vercel

**Открой:** Vercel Dashboard → Settings → Environment Variables

**Проверь наличие всех переменных:**

#### Обязательные (критично):
- ✅ `NEXT_PUBLIC_SUPABASE_URL` — должен быть URL типа `https://xxx.supabase.co`
- ✅ `NEXT_PUBLIC_SUPABASE_ANON_KEY` — должен быть длинный ключ (начинается с `eyJ`)
- ✅ `SUPABASE_SERVICE_ROLE_KEY` — должен быть длинный ключ (начинается с `eyJ`)
- ✅ `OPENAI_API_KEY` — должен начинаться с `sk-`

#### Для платежей:
- ✅ `YK_SHOP_ID` — число (например: `1163961`)
- ✅ `YK_SECRET_KEY` — строка (начинается с `test_` или `live_`)

#### Для приложения:
- ✅ `NEXT_PUBLIC_APP_URL` — должен быть `https://chatedem.com`

**Если какой-то переменной нет:**
1. Получи значение из Supabase/OpenAI/YooKassa
2. Добавь в Vercel: Settings → Environment Variables → Add
3. Выбери окружения: Production, Preview, Development
4. Сохрани

**Важно:** После добавления переменных нужно передеплоить проект!

---

### ШАГ 8: Проверь Supabase миграции

1. Открой: https://supabase.com/dashboard
2. Найди проект (URL должен совпадать с `NEXT_PUBLIC_SUPABASE_URL`)
3. Открой: SQL Editor
4. Проверь наличие таблиц: Table Editor

**Должны быть таблицы:**
- ✅ `profiles`
- ✅ `chat_sessions`
- ✅ `chat_messages`
- ✅ `billing_subscriptions`

**Если таблиц нет:**
1. Открой файл: `SUPABASE_FULL_MIGRATION.sql`
2. Скопируй весь SQL код
3. Вставь в Supabase SQL Editor
4. Выполни (Run)

**Проверь:**
- Все таблицы созданы?
- Есть ли данные в таблице `profiles`?

---

### ШАГ 9: Запусти деплой на Vercel

**Вариант 1: Автоматический деплой (если Git подключен)**

После `git push` Vercel автоматически задеплоит через 2-5 минут.

**Проверь:**
1. Vercel Dashboard → Deployments
2. Дождись завершения деплоя (статус: Ready)
3. Если ошибка — посмотри логи

**Вариант 2: Ручной деплой через CLI**

```bash
cd /Users/sanecek/tema/ai-chat-pro
npm i -g vercel
vercel login
vercel --prod
```

**Проверь:**
- Деплой завершился успешно?
- Статус: Ready?

---

### ШАГ 10: Проверь работу сайта

1. Открой: https://chatedem.com
2. Проверь:
   - ✅ Страница загружается?
   - ✅ Дизайн отображается?
   - ✅ Есть переключатель языка (RU/EN)?

3. Зарегистрируйся:
   - ✅ Регистрация работает?
   - ✅ Приходит email подтверждения?

4. Войди в систему:
   - ✅ Вход работает?

5. Отправь сообщение в чат:
   - ✅ Чат работает?
   - ✅ Приходит ответ от ИИ?
   - ✅ Нет ошибки 401?

6. Проверь локализацию:
   - ✅ Переключение языка работает?
   - ✅ ИИ отвечает на выбранном языке?

---

### ШАГ 11: Проверь логи на Vercel

1. Vercel Dashboard → Functions → Logs
2. Посмотри последние логи
3. Проверь:
   - ✅ Нет ошибок 401?
   - ✅ Нет ошибок 500?
   - ✅ Нет ошибок OpenAI API?

**Если есть ошибки:**
- Скопируй текст ошибки
- Найди причину в коде
- Исправь
- Повтори ШАГ 5-9

---

## 🔍 ДИАГНОСТИКА ПРОБЛЕМ

### Проблема: Ошибка 401 "Your authentication token is not from a valid issuer"

**Причина:** Используется `supabaseServiceKey` вместо `supabaseAnonKey` для проверки токенов.

**Решение:**
1. Открой файл с ошибкой (обычно `src/app/api/chat/stream/route.ts`)
2. Найди строку: `createClient(supabaseUrl, supabaseServiceKey)`
3. Замени на: `createClient(supabaseUrl, supabaseAnonKey)`
4. Сохрани
5. Повтори ШАГ 5-9

---

### Проблема: Ошибка 500 "Internal Server Error"

**Причина:** Отсутствует переменная окружения или ошибка в коде.

**Решение:**
1. Проверь логи на Vercel (Functions → Logs)
2. Найди точную ошибку
3. Если "Variable not found":
   - Проверь переменные окружения на Vercel (ШАГ 7)
   - Добавь недостающую переменную
   - Передеплой проект
4. Если другая ошибка:
   - Исправь код
   - Повтори ШАГ 5-9

---

### Проблема: Сайт не обновляется (старая версия)

**Причина:** Vercel не подключен к Git или деплой не запустился.

**Решение:**
1. Проверь подключение Git (ШАГ 6)
2. Проверь последний деплой (Vercel Dashboard → Deployments)
3. Если деплой старый:
   - Запусти ручной деплой (ШАГ 9, Вариант 2)
4. Если деплой новый, но сайт старый:
   - Очисти кеш браузера (Ctrl+Shift+R)
   - Проверь домен (должен быть `chatedem.com`)

---

### Проблема: Нет дизайна (белая страница)

**Причина:** Неправильный импорт Tailwind CSS.

**Решение:**
1. Проверь файл: `src/app/globals.css`
2. Должно быть: `@import "tailwindcss";`
3. Если неправильно — исправь
4. Повтори ШАГ 5-9

---

### Проблема: Чат не отвечает

**Причина:** Отсутствует `OPENAI_API_KEY` или неправильный ключ.

**Решение:**
1. Проверь переменную `OPENAI_API_KEY` на Vercel (ШАГ 7)
2. Проверь, что ключ начинается с `sk-`
3. Если ключ неправильный:
   - Получи новый ключ: https://platform.openai.com/api-keys
   - Обнови на Vercel
   - Передеплой проект

---

## ✅ ЧЕКЛИСТ ПРОВЕРКИ

После выполнения всех шагов проверь:

- [ ] Код скомпилирован без ошибок (`npm run build`)
- [ ] Все API роуты используют `supabaseAnonKey` для проверки токенов
- [ ] Все изменения закоммичены и запушены в Git
- [ ] Vercel подключен к Git репозиторию
- [ ] Все переменные окружения добавлены на Vercel
- [ ] Supabase миграции выполнены (все таблицы созданы)
- [ ] Деплой на Vercel завершился успешно
- [ ] Сайт открывается: https://chatedem.com
- [ ] Регистрация и вход работают
- [ ] Чат работает (нет ошибки 401)
- [ ] ИИ отвечает на сообщения
- [ ] Локализация работает (RU/EN)
- [ ] Нет ошибок в логах Vercel

---

## 🎯 ИТОГОВЫЙ ОТЧЁТ

После выполнения всех шагов создай отчёт:

```markdown
# Отчёт о выполнении задачи

**Дата:** [дата и время]
**Агент:** ИИ-агент

## Выполненные шаги:
- [x] ШАГ 1: Проверка состояния проекта
- [x] ШАГ 2: Проверка компиляции
- [x] ШАГ 3: Проверка аутентификации
- [x] ШАГ 4: Проверка API роутов
- [x] ШАГ 5: Коммит изменений
- [x] ШАГ 6: Проверка Vercel Git
- [x] ШАГ 7: Проверка переменных окружения
- [x] ШАГ 8: Проверка Supabase миграций
- [x] ШАГ 9: Деплой на Vercel
- [x] ШАГ 10: Проверка работы сайта
- [x] ШАГ 11: Проверка логов

## Найденные проблемы:
1. [описание проблемы, если была]
2. [описание проблемы, если была]

## Исправления:
1. [что исправлено]
2. [что исправлено]

## Текущий статус:
- ✅ Сайт работает: https://chatedem.com
- ✅ Чат работает (нет ошибки 401)
- ✅ ИИ отвечает
- ✅ Локализация работает
- ✅ Нет критических ошибок

## Что дальше:
[рекомендации, если есть]
```

---

## 🚀 НАЧНИ С ШАГА 1!

Выполняй шаги по порядку. Если на каком-то шаге возникла проблема — используй раздел "ДИАГНОСТИКА ПРОБЛЕМ".

**Важно:** Не пропускай шаги! Каждый шаг важен для работоспособности системы.


