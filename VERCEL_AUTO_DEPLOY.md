# 🚀 VERCEL АВТОМАТИЧЕСКИЙ ДЕПЛОЙ

## ✅ КАК ЭТО РАБОТАЕТ

**Vercel автоматически деплоит при каждом `git push` в основную ветку.**

### Процесс:

1. **Ты делаешь `git push`** → изменения отправляются в GitHub/GitLab
2. **GitHub отправляет webhook** → Vercel получает уведомление о новом коммите
3. **Vercel автоматически запускает деплой** → собирает проект и деплоит
4. **Готово через 2-3 минуты** → сайт обновлён

---

## 📋 НАСТРОЙКИ В VERCEL

### Проверь в Vercel Dashboard:

1. Открой https://vercel.com/dashboard
2. Выбери проект `ai-chat-pro` или `chatedem.com`
3. Перейди в **Settings → Git**
4. Там будет:
   - **Production Branch**: `main` (или `master`)
   - **Auto-deploy**: включено ✅

### Что деплоится автоматически:

- ✅ **Production Branch** (`main`) → деплоится на продакшен
- ✅ **Preview Branches** → деплоится как preview (опционально)
- ❌ **Другие ветки** → не деплоятся (если не настроено)

---

## 🔧 КАК УПРАВЛЯТЬ

### Отключить автоматический деплой:

1. Vercel Dashboard → Settings → Git
2. Отключи **"Auto-deploy"**
3. Теперь деплой только вручную через Dashboard

### Деплой вручную:

1. Vercel Dashboard → Deployments
2. Нажми **"Redeploy"** на нужном деплое
3. Или создай новый деплой из конкретного коммита

### Деплой только при теге:

1. Settings → Git → Production Branch
2. Установи **"Deploy only when a tag is pushed"**
3. Теперь деплой только при `git tag` и `git push --tags`

---

## ⚡ ТЕКУЩАЯ СИТУАЦИЯ

**У тебя сейчас:**
- ✅ Автоматический деплой включен
- ✅ Каждый `git push` → автоматический деплой
- ✅ Время деплоя: 2-3 минуты

**Это нормально и правильно!** Так и должно работать.

---

## 🎯 ЧТО ДЕЛАТЬ

### Если хочешь, чтобы деплоилось автоматически (как сейчас):
**Ничего не делай** — всё работает правильно ✅

### Если хочешь деплоить только вручную:
1. Открой Vercel Dashboard
2. Settings → Git → отключи Auto-deploy
3. Теперь деплой только через кнопку "Redeploy"

### Если хочешь деплоить только при тегах:
1. Settings → Git → Production Branch
2. Включи "Deploy only when a tag is pushed"
3. Теперь деплой только при `git tag v1.0.0 && git push --tags`

---

## 📊 ПРОВЕРКА

**Проверь последние деплои:**
1. Vercel Dashboard → Deployments
2. Там будут все деплои с временем и статусом
3. Каждый деплой = один коммит в `main`

**Если видишь деплои после каждого `git push`** → всё работает правильно ✅

---

**Вывод: Да, Vercel автоматически деплоит при каждом push. Это нормально и удобно!**


