# 🔐 Настройка админ-аккаунта

## Шаг 1: Запустить миграцию в Supabase

1. Открой Supabase Dashboard: https://supabase.com/dashboard
2. Выбери проект
3. SQL Editor → New query
4. Скопируй содержимое файла: `supabase/migrations/002_add_admin.sql`
5. Вставь и нажми Run

---

## Шаг 2: Сделать себя админом

### Вариант A: Через SQL Editor (быстро)

1. Supabase Dashboard → SQL Editor
2. Выполни SQL (замени EMAIL на свой):

```sql
-- Сделать себя админом
UPDATE profiles 
SET role = 'admin' 
WHERE email = 'твой@email.com';
```

3. Проверь:
```sql
-- Проверить что роль установлена
SELECT email, role FROM profiles WHERE email = 'твой@email.com';
```

### Вариант B: Через админ-панель (после первого входа)

1. Сначала сделай себя админом через SQL (Вариант A)
2. Войди на https://chatedem.com/login
3. Открой https://chatedem.com/admin
4. Введи email другого пользователя
5. Нажми "Назначить"

---

## Шаг 3: Войти в админ-панель

1. Открой https://chatedem.com/admin
2. Если не авторизован — перенаправит на `/login`
3. После входа увидишь админ-панель

---

## 📊 Что в админ-панели

- **Статистика:**
  - Количество пользователей
  - Pro подписки
  - Сессии чата
  - Сообщений

- **Промпты:**
  - Видишь текущий системный промпт
  - Где он находится (файл и строки)

- **Управление:**
  - Назначить других пользователей админами

---

## 📝 Промпты

**Где находятся:** `src/app/api/chat/route.ts` (строки 73-79)

**Текущий промпт:**
```
Ты — ИИ-зеркало для самопознания. Твоя задача:
- Видеть и отражать истину без оценок
- Помогать человеку понять себя через вопросы и наблюдения
- Быть мягким, но честным
- Помнить контекст разговора

Отвечай на русском языке, коротко и по существу.
```

**Как изменить:**
1. Открой `src/app/api/chat/route.ts`
2. Найди строку 73: `const systemPrompt = ...`
3. Измени текст
4. Задеплой: `git add . && git commit -m "Update prompt" && git push`

---

## ✅ Готово!

Теперь у тебя есть админ-доступ к https://chatedem.com/admin

